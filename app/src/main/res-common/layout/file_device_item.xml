<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackground"
    tools:context=".ui.dialog.common.FileDialog">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="@dimen/dp_15"
        android:paddingVertical="@dimen/dp_13">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/iv_file_device_icon"
            android:layout_width="@dimen/dp_24"
            android:layout_height="@dimen/dp_24"
            android:src="@drawable/file_interior" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_file_device_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/dp_10"
            android:layout_weight="1"
            android:ellipsize="middle"
            android:singleLine="true"
            android:textColor="@color/common_confirm_text_color"
            android:textSize="@dimen/sp_14"
            tools:text="内部存储" />

    </LinearLayout>

    <View
        android:id="@+id/v_file_device_line"
        android:layout_width="match_parent"
        android:layout_height="@dimen/line_size"
        android:layout_gravity="bottom"
        android:layout_marginStart="@dimen/dp_49"
        android:background="@color/common_line_color" />

</FrameLayout>

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/dp_12"
    android:paddingVertical="@dimen/dp_6">

    <!-- 卡片容器 -->
    <LinearLayout
        android:id="@+id/ll_download_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/chrome_download_card_bg"
        android:gravity="center_vertical"
        android:minHeight="@dimen/dp_72"
        android:orientation="horizontal"
        android:paddingHorizontal="@dimen/dp_14"
        android:paddingVertical="@dimen/dp_12">

        <!-- 文件类型图标 -->
        <ImageView
            android:id="@+id/iv_download_file_icon"
            android:layout_width="@dimen/dp_44"
            android:layout_height="@dimen/dp_44"
            android:layout_marginEnd="@dimen/dp_12"
            android:scaleType="centerInside" />

        <!-- 中间：文件名 + 大小/状态 -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- 文件名（正常：蓝色，删除：灰色+删除线） -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/tv_download_filename"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:ellipsize="middle"
                    android:maxLines="1"
                    android:textSize="@dimen/sp_14" />

                <!-- 已删除标签 -->
                <TextView
                    android:id="@+id/tv_download_deleted_tag"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/dp_8"
                    android:text="已删除"
                    android:textColor="@color/chrome_url_hint_color"
                    android:textSize="@dimen/sp_12"
                    android:visibility="gone" />

            </LinearLayout>

            <!-- 文件大小 / 下载中的网速信息 -->
            <TextView
                android:id="@+id/tv_download_info"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/dp_4"
                android:textColor="@color/chrome_url_hint_color"
                android:textSize="@dimen/sp_12"
                android:visibility="gone" />

            <!-- 下载中：网速信息（蓝色） -->
            <TextView
                android:id="@+id/tv_download_speed_info"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/dp_2"
                android:textColor="@color/chrome_secure_color"
                android:textSize="@dimen/sp_12"
                android:visibility="gone" />

            <!-- 进度条（仅下载中） -->
            <ProgressBar
                android:id="@+id/pb_download_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="@dimen/dp_4"
                android:layout_marginTop="@dimen/dp_6"
                android:progressDrawable="@drawable/download_progress_bg"
                android:visibility="gone" />

        </LinearLayout>

        <!-- 删除按钮 -->
        <ImageView
            android:id="@+id/iv_download_delete"
            android:layout_width="@dimen/dp_32"
            android:layout_height="@dimen/dp_32"
            android:layout_marginStart="@dimen/dp_8"
            android:background="@drawable/chrome_icon_btn_bg"
            android:padding="@dimen/dp_8"
            android:src="@drawable/chrome_close_ic"
            app:tint="@color/chrome_icon_color" />

    </LinearLayout>

</LinearLayout>
